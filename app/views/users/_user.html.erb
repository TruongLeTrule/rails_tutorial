<li>
  <%= gravatar_for user, size: Settings.avatar_size %>
  <%= link_to user.name, user %>
  <% if can_destroy_user? user %>
    | <%= link_to t(".delete", default: "Delete"), user, data: { "turbo-method": :delete, turbo_confirm: t(".delete_confirm")} %>
  <% end %>
</li>
